<div class="row">
	<% @products.each do |product| %>
		<div class="col-md-3">
			<a href="/products/<%= product.id %>">
			<%= image_tag product.image.url,  class: "book"%>
			</a>
			</div>
	<% end %>
</div>
